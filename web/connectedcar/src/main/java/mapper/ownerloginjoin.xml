<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="connected.car.weblogin">

	<select id="shoplogin" resultType="ownervo" parameterType="ownervo">
		select *
		from repair_shop_user
		where owner_id = #{owner_id} and owner_password = #{owner_password}
	</select>
	<!-- <insert id="register">
		insert into member
		values(#{member_id}, #{pass},
		#{name},#{ssn},#{phonenum},#{height},#{weight},#{gender},'seed',0,0,sysdate,'test')
	</insert>
	<select id="idCheck" resultType="member" parameterType="String">
		select * from member where member_id = #{member_id}
	</select> -->


</mapper>